class Solution:
    def maxDistance(self, colors):
        n=len(colors)
        for j in range(n-1,-1,-1):
            if colors[j]!=colors[0]:
                a=j
                break
        for i in range(n):
            if colors[i]!=colors[n-1]:
                b=n-1-i
                break
        return max(a,b)
